import { LogMeta } from '../LogMeta';
import { Formatter } from './Formatter';
export interface DefaultFormatterOptions {
    dateFormat?: string;
    colorErrors?: boolean;
    colorMeta?: boolean;
}
export declare class DefaultFormatter extends Formatter {
    private readonly errorColorer;
    private readonly logColorer;
    private readonly options;
    protected readonly STORAGE_KEY: symbol;
    constructor(options?: DefaultFormatterOptions);
    protected formatMessage(meta: LogMeta, message: string): string;
    protected formatError(meta: LogMeta, error: Error): string;
    private getCausePrefix;
    private colorError;
}
