/**
 * @ignore
 */
export declare class PackageDetector {
    private readonly packageCache;
    getRootOf(directory: string): string;
    getInfo(rootDir: string): any;
    getCallerDirectory(): string;
    private getCallStack;
    private readdirSync;
    private require;
}
