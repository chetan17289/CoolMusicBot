import { TransportOptions } from '../transports';
export declare class V1 {
    static setConfig(config: TransportOptions & {
        formatter?: never;
    }): void;
    private static remapLoggers;
    private static remapLevel;
}
